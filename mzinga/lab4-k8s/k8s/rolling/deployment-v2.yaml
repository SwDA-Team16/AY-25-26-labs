apiVersion: apps/v1
kind: Deployment
metadata:

  # [Lab 4 - Step 3.2] Same Deployment name triggers an update
  name: webapp
  namespace: mzinga-lab4
  labels:
    app: webapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  strategy:

    # [Lab 4 - Step 2.1] RollingUpdate gradually replaces Pods
    type: RollingUpdate
    rollingUpdate:

      # [Lab 4 - Step 2.1] maxUnavailable limits Pods taken offline
      maxUnavailable: 1

      # [Lab 4 - Step 2.1] maxSurge allows temporary extra Pods
      maxSurge: 1
  template:
    metadata:
      labels:
        app: webapp
        version: v2
    spec:
      containers:
        - name: webapp
          image: mzinga-webapp:2.0.0

          # [Lab 4 - Step 2.1] Use local images loaded into minikube
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: APP_VERSION
              value: "2.0.0"
            - name: APP_COLOR
              value: "green"

          # [Lab 4 - Step 2.1] Readiness and liveness probes use /health
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "32Mi"
            limits:
              cpu: "100m"
              memory: "64Mi"
